Write clean, executable Python Manim (Community Edition) code for a single scene.
Define a class named GeneratedScene inheriting from Scene: `class GeneratedScene(Scene):`.
Imitate the provided verified reference snippets. Use relative positioning methods (`next_to`, `align_to`, `arrange`).
Do not use literal coordinates e.g. np.array([x, y, z]).
Do not import os, sys, subprocess, or execute dangerous operations.
